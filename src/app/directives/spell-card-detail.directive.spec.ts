import { SpellCardDetailDirective } from './spell-card-detail.directive';

describe('SpellCardDetailDirective', () => {
  it('should create an instance', () => {
    const directive = new SpellCardDetailDirective();
    expect(directive).toBeTruthy();
  });
});
