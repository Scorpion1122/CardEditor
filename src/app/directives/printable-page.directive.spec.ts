import { PrintablePageDirective } from './printable-page.directive';

describe('PrintablePageDirective', () => {
  it('should create an instance', () => {
    const directive = new PrintablePageDirective();
    expect(directive).toBeTruthy();
  });
});
